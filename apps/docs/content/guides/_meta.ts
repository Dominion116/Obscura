import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "read-the-registry": "Read the Registry",
  wrap: "Wrap Tokens",
  unwrap: "Unwrap Tokens",
  "decrypt-a-balance": "Decrypt a Balance",
  transfer: "Confidential Transfer",
};

export default meta;
