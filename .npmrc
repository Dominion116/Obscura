# The wallet stack has unresolved peer ranges (abitype wants zod@^3 while the
# app uses zod@4; use-sync-external-store pins react<=18 under react 19).
# pnpm tolerated these; npm needs legacy resolution to install.
legacy-peer-deps=true
